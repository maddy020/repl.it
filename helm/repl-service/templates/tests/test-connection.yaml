apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "repl-service.fullname" . }}-test-connection"
  labels:
    {{- include "repl-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "repl-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
