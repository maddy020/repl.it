apiVersion: v1
kind: Service
metadata:
  name: {{ include "repl-service.fullname" . }}
  labels:
    {{- include "repl-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "repl-service.selectorLabels" . | nindent 4 }}
